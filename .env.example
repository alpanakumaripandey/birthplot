# Optional LLM for Match "Simple summary" (Token Harbor / OpenAI-compatible)
# Copy to `.env` and fill in — never commit `.env`.

LLM_API_KEY=
LLM_BASE_URL=https://tokenharbor.ai/v1
LLM_MODEL=deepseek-v4-flash:free
